#ifndef ENUMS_H
#define ENUMS_H

// Enum of object type
enum class ObjectType
{
    PLAYER_TYPE,
    BULLET_TYPE,
    ASTEROID_TYPE,
    STAR,
    UNKNOWN_TYPE
};

enum class ObjectShape
{
    Circle,
    Rectangle,
    Triangle,
    Lines
};

#endif // ENUMS_H

